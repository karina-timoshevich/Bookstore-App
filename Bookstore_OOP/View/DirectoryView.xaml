<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Bookstore_OOP.View.DirectoryView"
             xmlns:model="clr-namespace:Bookstore_OOP.Model"
             xmlns:xct="clr-namespace:CommunityToolkit.Maui.Behaviors;assembly=CommunityToolkit.Maui"
             Title="Shop">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <SearchBar Grid.Row="0"
                   FontSize="Medium"
                   Placeholder="Search..."
                   Text="{Binding SearchText, Mode=TwoWay}"
                   HeightRequest="50"
                   VerticalOptions="StartAndExpand">

            <SearchBar.Behaviors>
                <xct:EventToCommandBehavior EventName="TextChanged"
                                             Command="{Binding SearchBookCommand}"/>
            </SearchBar.Behaviors>

        </SearchBar>

        <!-- Book List -->
        <ListView Grid.Row="1"
                  HasUnevenRows="True"
                  ItemsSource="{Binding Books}"
                  SelectedItem="{Binding SelectedBook}"
                  BackgroundColor="Transparent"
                  VerticalScrollBarVisibility="Never"
                  HorizontalScrollBarVisibility="Never"
                  ItemSelected="OnItemSelected"
                  Header="{x:Null}">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="{x:Type model:BookDisplay}">

                    <ViewCell>
                        <Frame BorderColor="BlueViolet" CornerRadius="5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Book Cover -->
                                <Image Grid.Row="0" Grid.Column="0" Source="{Binding Book.CoverPath}" Aspect="AspectFit" />

                                <Label Grid.Row="0" Grid.Column="1" Text="{Binding Book.Title}" FontSize="Medium" VerticalOptions="Center" HorizontalTextAlignment="Center" Margin="10,0,0,0" />
                                <!--<Label Grid.Column="2" Text="{Binding AuthorName}" FontSize="Medium" VerticalOptions="Center" />-->
                                <Label Grid.Row="1" Grid.Column="1" Text="{Binding Book.Price, StringFormat='{0} rub'}" FontSize="Medium" VerticalOptions="Center" HorizontalTextAlignment="Center" Margin="10,0,0,0" />
                                <Button Grid.Row="2" Grid.Column="1" TextColor="White" Text="ADD TO CART"
                            Command="{Binding Path=BindingContext.AddToCartCommand, Source={RelativeSource AncestorType={x:Type ContentPage}}}"
                            CommandParameter="{Binding}"
                            Clicked="OnAddToCartClicked" Margin="10,0,0,0" />
                            </Grid>
                        </Frame>

                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</ContentPage>